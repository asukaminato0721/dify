FastAPI Port Roadmap
====================

Last updated: 2026-04-18

Done
----
- Copied the existing `../api` backend source into `api-new` as migration material.
- Replaced the stub entrypoint with a FastAPI/Uvicorn app in `main.py`.
- Added a FastAPI app factory with lifespan management in `app_factory.py`.
- Added request/tracing middleware in `api_server/middleware.py`.
- Replaced the Flask database bootstrap with an async SQLAlchemy manager in `extensions/ext_database.py`.
- Added a FastAPI-native exception layer in `api_server/errors.py`.
- Added FastAPI-native JWT/passport helpers in `api_server/auth.py`.
- Switched the active public webapp auth extractors to `fastapi.security` helpers instead of Flask-Login-style request parsing.
- Added a FastAPI-local ORM base and SQLAlchemy types in `api_server/db.py` to avoid importing the legacy `models` package.
- Replaced eager `models/__init__.py` imports with a lazy loader so the package no longer imports the full Flask-era ORM graph at import time.
- Removed Flask compatibility dependencies from `api-new` after confirming the active FastAPI slice does not need them.
- Ported health endpoints:
  - `GET /health`
  - `GET /health/live`
  - `GET /health/ready`
- Ported bootstrap/public endpoints:
  - `GET /console/api/ping`
  - `GET /console/api/version`
  - `GET /console/api/system-features`
  - `GET /api/system-features`
  - `GET /console/api/init`
  - `POST /console/api/init`
  - `GET /console/api/setup`
  - `POST /console/api/setup`
- Ported public webapp endpoints:
  - `GET /api/webapp/access-mode`
  - `GET /api/webapp/permission`
  - `GET /api/site`
  - `GET /api/parameters`
  - `GET /api/meta`
  - `GET /api/passport`
  - `GET /api/login/status`
  - `POST /api/logout`
  - `POST /api/login`
  - `POST /api/email-code-login`
  - `POST /api/email-code-login/validity`
  - `POST /api/forgot-password`
  - `POST /api/forgot-password/validity`
  - `POST /api/forgot-password/resets`
  - `GET /api/remote-files/{url}`
  - `POST /api/remote-files/upload`
  - `POST /api/files/upload`
  - `GET /api/conversations`
  - `DELETE /api/conversations/{conversation_id}`
  - `POST /api/conversations/{conversation_id}/name`
  - `PATCH /api/conversations/{conversation_id}/pin`
  - `PATCH /api/conversations/{conversation_id}/unpin`
  - `GET /api/messages`
  - `POST /api/messages/{message_id}/feedbacks`
  - `GET /api/saved-messages`
  - `POST /api/saved-messages`
  - `DELETE /api/saved-messages/{message_id}`
  - `POST /api/completion-messages/{task_id}/stop`
  - `POST /api/chat-messages/{task_id}/stop`
  - `POST /api/workflows/tasks/{task_id}/stop`
  - `GET /files/{file_id}/image-preview`
  - `GET /files/{file_id}/file-preview`
  - `GET /files/workspaces/{workspace_id}/webapp-logo`
  - `GET /api/form/human_input/{form_token}`
  - `POST /api/form/human_input/{form_token}`
  - `POST /api/audio-to-text`
  - `POST /api/text-to-audio`
  - `GET /api/messages/{message_id}/suggested-questions`
  - `GET /api/messages/{message_id}/more-like-this`
- Added an async bootstrap service in `api_server/services/bootstrap.py` for first-time setup and init validation.
- Added a minimal async app/site lookup service in `api_server/services/app_lookup.py`.
- Added a FastAPI-native webapp context service in `api_server/services/webapp_context.py`.
- Added local public webapp models in `api_server/models/app.py` for app, tenant, site, end-user, app config, workflow, and API tool provider lookups.
- Added a local async `WorkflowRun` model in `api_server/models/workflow.py` for workflow event replay without importing the legacy ORM graph.
- Added a FastAPI-native workflow event replay service in `api_server/services/workflow_events.py`.
- Added a FastAPI-native public generation bridge in `api_server/services/generation_bridge.py`.
- Added an async-native completion/chat generation service in `api_server/services/generation.py`.
- Extended the async-native generation service to support completion `more-like-this`.
- Added async event emission APIs on `EventManager`, `GraphEngine`, and `WorkflowEntry` as the execution-engine path forward for the remaining workflow runtime port.
- Added async runner entrypoints on `WorkflowAppRunner` and `AdvancedChatAppRunner` to consume the new async graph-event surface.
- Added async-native ready-queue, dispatcher, and worker-pool primitives for the graph engine control plane.
- Added async node execution entrypoints on the graph node base class and switched the async worker pool to consume them.
- Added async pooled HTTP client support and async plugin model-client methods for the lower model/provider runtime layer.
- Added async model-runtime/model-instance methods for schema lookup, LLM invoke, and token counting in the lower runtime abstraction layer.
- Added async LLM-node execution hooks on the lower runtime stack, including an async node entrypoint and async LLM-node invoke path.
- Extended the async LLM-backed node path to `question_classifier` and `parameter_extractor`.
- Added an async transport/runtime path for the `http_request` node.
- Added async invoke/runtime entrypoints for the `tool` node stack.
- Removed the active FastAPI generation route dependency on the legacy Flask generation stack; authenticated generation now returns an explicit FastAPI-side `503` until a native backend is ported.
- Added local async human-input form models in `api_server/models/human_input.py` for public form token access without the legacy Flask repositories.
- Added a FastAPI-native human-input form service in `api_server/services/human_input_forms.py`.
- Added a FastAPI-native public audio service in `api_server/services/audio.py`.
- Added a FastAPI-native suggested-questions service in `api_server/services/suggested_questions.py`.
- Rewired the public `more-like-this` message route through the async-native completion generation service.
- Initialized Redis pub/sub for the FastAPI app lifespan via the existing Redis extension bootstrap.
- Mounted public generation endpoints on FastAPI route handlers:
  - `POST /api/completion-messages`
  - `POST /api/chat-messages`
  - `POST /api/workflows/run`
- Rewired public completion/chat generation endpoints through the async-native FastAPI generation service.
- Replaced the public workflow route's hard-coded `generation_backend_unavailable` stub with a direct workflow-runner path that no longer depends on Flask compatibility glue.
- Replaced the public advanced-chat and agent-chat route-level `generation_backend_unavailable` path with the same compatibility bridge.
- Replaced the public advanced-chat route's broad `AppGenerateService` compatibility bridge with a direct runner/task-pipeline path that avoids the Flask controller layer.
- Replaced the public agent-chat route's broad `AppGenerateService` compatibility bridge with a direct runner/task-pipeline path that avoids the Flask controller layer.
- Wired the async database bootstrap to configure a sync companion SQLAlchemy session factory for the copied workflow runtime seams that are still active under FastAPI.
- Switched the active advanced-chat/workflow sync helper paths off direct `db.engine`/`db.session` assumptions and onto the configured sync companion session factory.
- Switched the active agent-chat/message-pipeline sync helper paths off direct `db.engine`/`db.session` assumptions and onto the configured sync companion session factory.
- Switched the active agent workflow-node runtime helpers off direct `db.engine` usage and onto the configured sync companion session factory.
- Removed the now-dead public generation Flask compatibility shim from `api_server/services/generation.py`.
- Removed the active message-cycle conversation-name worker's Flask app-context dependency from the public generation path.
- Switched the shared workflow response/pipeline fallback helpers off ad hoc `db.engine.sync_engine` probing and onto the configured sync companion session factory.
- Switched the copied message-based generator helpers and the copied workflow/advanced-chat generators off direct `db.engine`/`db.session` assumptions and onto the configured sync companion session factory.
- Removed the copied workflow and advanced-chat generator workers' Flask request-context dependency; those workers now run from explicit arguments plus the configured session factory.
- Removed the copied agent and chat generator workers' Flask request-context dependency, and switched the copied chat/completion runners to the configured sync session factory for app lookups.
- Removed the copied completion generator's remaining Flask worker-context dependency, including the copied `more_like_this` branch.
- Removed the copied pipeline generator/runner Flask worker-context dependency and switched the copied pipeline stack to the configured sync session factory at its database boundary.
- Rewired public completion `more-like-this` through the async-native FastAPI generation service.
- Ported public workflow event replay endpoint:
  - `GET /api/workflow/{task_id}/events`
- Ported public human-input form endpoints:
  - `GET /api/form/human_input/{form_token}`
  - `POST /api/form/human_input/{form_token}`
- Ported public audio endpoints:
  - `POST /api/audio-to-text`
  - `POST /api/text-to-audio`
- Ported public message suggestion endpoint:
  - `GET /api/messages/{message_id}/suggested-questions`
- Mounted public message `more-like-this` endpoint:
  - `GET /api/messages/{message_id}/more-like-this`
- Added FastAPI-native app-token auth and resource lookup services for the initial `/v1` service API slice.
- Added FastAPI-native service API app/file services plus local end-user resolution for the growing `/v1` slice.
- Ported initial service API endpoints:
  - `GET /v1/`
  - `GET /v1/site`
  - `GET /v1/end-users/{end_user_id}`
- Ported additional service API app/file endpoints:
  - `GET /v1/parameters`
  - `GET /v1/meta`
  - `GET /v1/info`
  - `POST /v1/files/upload`
  - `GET /v1/files/{file_id}/preview`
- Ported additional service API message endpoints:
  - `GET /v1/messages`
  - `POST /v1/messages/{message_id}/feedbacks`
  - `GET /v1/messages/{message_id}/suggested`
- Ported additional service API conversation endpoints:
  - `GET /v1/conversations`
  - `DELETE /v1/conversations/{conversation_id}`
  - `POST /v1/conversations/{conversation_id}/name`
- Ported additional service API generation/task-control endpoints:
  - `POST /v1/completion-messages`
  - `POST /v1/completion-messages/{task_id}/stop`
  - `POST /v1/chat-messages`
  - `POST /v1/chat-messages/{task_id}/stop`
- Ported additional service API workflow endpoints:
  - `POST /v1/workflows/run`
  - `GET /v1/workflows/run/{workflow_run_id}`
  - `POST /v1/workflows/tasks/{task_id}/stop`
- Ported additional service API audio endpoints:
  - `POST /v1/audio-to-text`
  - `POST /v1/text-to-audio`
- Ported additional service API feedback/workflow endpoints:
  - `GET /v1/app/feedbacks`
  - `POST /v1/workflows/{workflow_id}/run`
- Ported additional service API conversation-variable endpoints:
  - `GET /v1/conversations/{conversation_id}/variables`
  - `PUT /v1/conversations/{conversation_id}/variables/{variable_id}`
- Ported additional service API workflow-log endpoint:
  - `GET /v1/workflows/logs`
- Ported additional service API annotation endpoints:
  - `POST /v1/apps/annotation-reply/{action}`
  - `GET /v1/apps/annotation-reply/{action}/status/{job_id}`
  - `GET /v1/apps/annotations`
  - `POST /v1/apps/annotations`
  - `PUT /v1/apps/annotations/{annotation_id}`
  - `DELETE /v1/apps/annotations/{annotation_id}`
- Ported dataset-token service API workspace-models endpoint:
  - `GET /v1/workspaces/current/models/model-types/{model_type}`
- Ported dataset-token service API dataset metadata endpoints:
  - `GET /v1/datasets/{dataset_id}/metadata`
  - `GET /v1/datasets/{dataset_id}/metadata/built-in`
- Ported dataset-token service API dataset metadata mutation endpoints:
  - `POST /v1/datasets/{dataset_id}/metadata`
  - `PATCH /v1/datasets/{dataset_id}/metadata/{metadata_id}`
  - `DELETE /v1/datasets/{dataset_id}/metadata/{metadata_id}`
  - `POST /v1/datasets/{dataset_id}/metadata/built-in/{action}`
- Ported dataset-token service API dataset document metadata endpoint:
  - `POST /v1/datasets/{dataset_id}/documents/metadata`
- Ported dataset-token service API dataset tag endpoints:
  - `GET /v1/datasets/tags`
  - `POST /v1/datasets/tags`
  - `PATCH /v1/datasets/tags`
  - `DELETE /v1/datasets/tags`
  - `POST /v1/datasets/tags/binding`
  - `POST /v1/datasets/tags/unbinding`
  - `GET /v1/datasets/{dataset_id}/tags`
- Ported dataset-token service API dataset read endpoints:
  - `GET /v1/datasets`
  - `GET /v1/datasets/{dataset_id}`
- Ported dataset-token service API document read endpoints:
  - `GET /v1/datasets/{dataset_id}/documents`
  - `GET /v1/datasets/{dataset_id}/documents/{batch}/indexing-status`
  - `GET /v1/datasets/{dataset_id}/documents/{document_id}`
- Ported dataset-token service API document download endpoint:
  - `GET /v1/datasets/{dataset_id}/documents/{document_id}/download`
- Ported dataset-token service API document download endpoint:
  - `GET /v1/datasets/{dataset_id}/documents/{document_id}/download`
- Added a minimal FastAPI test file in `tests/test_app_routes.py`.
- Added focused auth extractor tests in `tests/test_auth_security.py`.
- Added focused service API route tests in `tests/test_service_api_routes.py`.
- Added focused human-input route tests in `tests/test_human_input_form.py`.
- Added focused audio route tests in `tests/test_audio_routes.py`.
- Added focused suggested-questions route tests in `tests/test_suggested_questions_route.py`.
- Added focused `more-like-this` route coverage in `tests/test_suggested_questions_route.py`.
- Added focused generation bridge tests in `tests/test_generation_bridge.py`.
- Added focused generation route tests in `tests/test_generation_routes.py`.
- Added focused generation compatibility tests in `tests/test_generation_service.py`.
- Extended focused generation compatibility tests to cover the new native advanced-chat path while keeping agent-chat on the compatibility bridge.
- Extended focused generation compatibility tests to cover the new native agent-chat path and its streaming-only contract.
- Switched the active public workflow/advanced-chat/agent-chat generation helpers off ad hoc `db.engine.sync_engine` probing and onto the configured sync companion session factory.
- Switched the active workflow response/log path off legacy `WorkflowRun`/`WorkflowAppLog` ORM classes and onto local FastAPI workflow models, including a write-compatible local workflow-app-log mapping for the copied public workflow pipeline.
- Switched the active workflow pause-token lookup path off legacy human-input ORM models and onto local FastAPI human-input mappings, while preserving recipient-priority token selection for paused workflow resume links.
- Switched the active public workflow generation loader off legacy `App`/`Workflow`/`EndUser` ORM models and onto the local FastAPI mappings, and widened the copied workflow runtime/config helpers on that path to accept the local workflow/end-user shapes.
- Extended the local FastAPI app/workflow/conversation/message models with the fields and helper methods required by the active advanced-chat runtime, and switched the active public advanced-chat path onto those local FastAPI models instead of the legacy ORM classes for its loader, config, runner, snapshots, and conversation-variable persistence.
- Extended the local FastAPI app/conversation/message models with the helper methods required by the active agent-chat runtime (`Conversation.app`, `Conversation.model_config`, and `Message.message_metadata_dict`), switched the public agent-chat loader and record creation path onto local FastAPI models, and updated the active token-memory / agent config / base-runner entry signatures to accept those local model shapes.
- Switched the active inner agent-chat runtime off `db.session` globals for agent-thought creation/update, prompt-history loading, tool-response message-file persistence, and easy-ui response file/agent-thought lookups; added a local FastAPI `MessageAgentThought` mapping plus local-model support through `ToolEngine`, `BaseAgentRunner`, `TokenBufferMemory`, and the easy-ui task pipeline.
- Widened the shared FastAPI generation/config boundary to use local `AppMode` and narrowed the active generation helpers in `api_server/services/generation.py` to the local FastAPI workflow/advanced-chat/agent-chat models on the public path, removing another layer of live legacy model dependence from the active generation entrypoints.
- Switched the shared message-cycle helper off legacy `Conversation`/`MessageFile` lookups and onto local FastAPI message/conversation models for assistant-file detection and conversation-name worker bootstrap on the active advanced/agent path.
- Completed the local `AppModelConfig.to_dict()` compatibility needed by the copied chat/completion config managers and widened those copied chat/completion config/runner entry signatures to accept local FastAPI `App`, `AppModelConfig`, `Conversation`, and `Message` models.
- Cleared the remaining stale legacy type dependencies in the shared chat/completion/agent runtime helpers (`AppRunner`, `TokenBufferMemory`, `ToolEngine`, `EasyUIBasedGenerateTaskPipeline`, and `MessageCycleManager`) so their focused runtime slices type-check cleanly with the new local FastAPI model shapes.
- Switched the shared message-based generator layer and the copied chat/completion generator entry signatures onto local FastAPI `App`, `AppModelConfig`, `Conversation`, `EndUser`, and `Message` models, and ported the conversation lookup on that copied chat path to an explicit sync session against the local FastAPI `Conversation` table.
- Added focused async graph-engine event tests in `tests/test_graph_engine_async.py`.
- Added focused async ready-queue tests in `tests/test_async_ready_queue.py`.
- Added focused async node execution tests in `tests/test_node_async.py`.
- Installed the new project environment with `uv sync`.
- Ran targeted type checks with `pyrefly` on the new FastAPI runtime slice; current targeted result is clean.
- Current active FastAPI runtime tests pass: `49 passed`.
- The expanded generation-focused verification slice now also passes: `36 passed`.
- Targeted `pyrefly` is clean for the updated generation bridge/service test slice.
- Targeted service API route/auth verification now also passes: `6 passed`.
- Targeted `pyrefly` is clean for the initial FastAPI `/v1` service API slice.
- Expanded targeted service API route/auth verification now also passes: `11 passed`.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file slice.
- Further expanded targeted service API route/auth verification now also passes: `14 passed`.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message slice.
- Further expanded targeted service API route/auth verification now also passes: `17 passed`.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation slice.
- Further expanded targeted service API route/auth verification now also passes: `21 passed`.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation slice.
- Further expanded targeted service API route/auth verification now also passes: `24 passed`.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow slice.
- Further expanded targeted service API route/auth verification now also passes: `26 passed`.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio slice.
- Further expanded targeted service API route/auth verification now also passes: `33 passed`.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio/feedback slice.
- Further expanded targeted service API route/auth verification now also passes: `30 passed` for the focused service-route slice.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio/feedback/variables slice.
- Further expanded targeted service API route/auth verification now also passes: `31 passed` for the focused service-route slice.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio/feedback/variables/logs slice.
- Further expanded targeted service API route/auth verification now also passes: `37 passed` for the focused service-route slice.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio/feedback/variables/logs/annotations slice.
- Further expanded targeted service API route/auth verification now also passes: `38 passed` for the focused service-route slice.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio/feedback/variables/logs/annotations/workspace-models slice.
- Further expanded targeted service API route/auth verification now also passes: `40 passed` for the focused service-route slice.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio/feedback/variables/logs/annotations/workspace-models/dataset-metadata slice.
- Further expanded targeted service API route/auth verification now also passes: `44 passed` for the focused service-route slice.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio/feedback/variables/logs/annotations/workspace-models/dataset-metadata-mutation slice.
- Further expanded targeted service API route/auth verification now also passes: `45 passed` for the focused service-route slice.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio/feedback/variables/logs/annotations/workspace-models/dataset-metadata-mutation/document-metadata slice.
- Further expanded targeted service API route/auth verification now also passes: `51 passed` for the focused service-route slice.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio/feedback/variables/logs/annotations/workspace-models/dataset-metadata-mutation/document-metadata/tags slice.
- Further expanded targeted service API route/auth verification now also passes: `53 passed` for the focused service-route slice.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio/feedback/variables/logs/annotations/workspace-models/dataset-metadata-mutation/document-metadata/tags/datasets slice.
- Further expanded targeted service API route/auth verification now also passes: `56 passed` for the focused service-route slice.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio/feedback/variables/logs/annotations/workspace-models/dataset-metadata-mutation/document-metadata/tags/datasets/documents slice.
- Further expanded targeted service API route/auth verification now also passes: `58 passed` for the focused service-route slice.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio/feedback/variables/logs/annotations/workspace-models/dataset-metadata-mutation/document-metadata/tags/datasets/documents/download slice.
- Further expanded targeted service API route/auth verification now also passes: `57 passed` for the focused service-route slice.
- Targeted `pyrefly` is clean for the expanded FastAPI `/v1` service API app/file/message/conversation/generation/workflow/audio/feedback/variables/logs/annotations/workspace-models/dataset-metadata-mutation/document-metadata/tags/datasets/documents/download slice.
- Focused generation service verification now also passes: `6 passed`.
- Targeted `pyrefly` is clean for the public generation session-factory seam slice.
- Focused workflow-model/generation/log verification now also passes: `12 passed`.
- Targeted `pyrefly` is clean for the active workflow-model isolation slice.
- Focused human-input-token/generation/form verification now also passes: `10 passed`.
- Targeted `pyrefly` is clean for the active human-input model isolation slice.
- Focused workflow-local-model/generation verification now also passes: `10 passed`.
- Targeted `pyrefly` is clean for the active local-workflow runtime compatibility slice.
- Focused advanced-chat local-model/generation verification now also passes: `10 passed`.
- Targeted `pyrefly` is clean for the active advanced-chat local-model compatibility slice.
- Focused agent local-model/generation verification now also passes: `9 passed`.
- Targeted `pyrefly` is clean for the local agent-model helper slice.
- Focused agent inner-runtime/local-model verification now also passes: `10 passed`.
- Targeted `pyrefly` is clean for the active agent runtime compatibility slice.
- Combined focused generation/advanced-chat/agent-chat local-model verification now also passes: `14 passed`.
- Targeted `pyrefly` is clean for the active generation/config boundary slice.
- Focused message-cycle verification now also passes: `2 passed`.
- Targeted `pyrefly` is clean for the shared message-cycle slice.
- Focused chat/completion local-model compatibility verification now also passes: `2 passed`.
- Targeted `pyrefly` is clean for the copied chat/completion config-runner compatibility slice.
- Combined focused shared runtime helper verification now also passes: `8 passed`.
- Targeted `pyrefly` is clean for the shared runtime helper slice.
- Focused chat/completion generator/local-model verification now also passes: `4 passed`.
- Targeted `pyrefly` is clean for the shared message-based generator and conversation-service slice.
- Moved the active workflow/advanced-chat/agent-chat request-stage generation prep onto `AsyncSession`, including local model loading, owned-conversation lookup, message/conversation record creation, and advanced-chat thread counting before the copied sync runtime boundary.
- Removed redundant sync app/conversation/message reloads in the active advanced-chat and agent-chat runners when FastAPI-local model instances are already available from the request-stage prep path.
- Fixed the active FastAPI completion/chat async message write helpers to commit after flush so message creation and answer persistence do not rely on implicit session behavior.
- Expanded focused generation verification now also passes: `16 passed` for the generation route/bridge/service slice and `8 passed` for the advanced-chat/agent-chat local-model slice.
- Targeted `pyrefly` is clean for the active generation request-stage async-prep slice.
- Added prefetched-cache support on the local FastAPI `App`/`AppModelConfig`-backed conversation and message helpers so the active agent path can reuse request-stage-loaded models instead of reopening sync sessions for those property lookups.
- Wired the active public agent-chat prep path to populate those local-model caches before entering the copied sync runner boundary.
- Expanded focused agent local-model/generation verification now also passes: `14 passed` for `tests/test_agent_chat_local_models.py` plus `tests/test_generation_service.py`.
- Targeted `pyrefly` is clean for the local-model cache support slice.
- Added async prefetch for the active agent-chat memory slice so recent conversation messages and their user/assistant message-file rows are loaded on `AsyncSession` before entering the copied sync runner boundary.
- Taught `TokenBufferMemory` to consume prefetched FastAPI-local history, message-file, app-model-config, and workflow caches before falling back to the sync session factory, reducing live sync reads on the active agent path.
- Expanded focused agent-memory verification now also passes: `15 passed` for `tests/test_token_buffer_memory.py`, `tests/test_agent_chat_local_models.py`, and `tests/test_generation_service.py`.
- Targeted `pyrefly` is clean for the active token-buffer memory cache slice.
- Added cache-first loading in `BaseAgentRunner` for agent-thought counts, thread history, per-message agent thoughts, user prompt files, and app-model-config lookup so the active FastAPI agent path can reuse request-stage-prefetched local models before falling back to sync sessions.
- Wired the active agent-chat request-stage prefetch to include cached agent-thought lists and per-message counts alongside the existing history/message-file caches.
- Expanded focused base-agent verification now also passes: `17 passed` for `tests/test_base_agent_runner.py`, `tests/test_token_buffer_memory.py`, `tests/test_agent_chat_local_models.py`, and `tests/test_generation_service.py`.
- Targeted `pyrefly` is clean for the active base-agent runner cache slice.
- Extended `QueueMessageFileEvent` to optionally carry message-file metadata from the publisher, and updated the active base-app publisher to populate that metadata when it already has the inserted row in hand.
- Removed the active message-cycle path’s sync lookup for `get_message_event_type`, and taught `message_file_to_stream_response` to use queue-event metadata first with a sync fallback only for legacy id-only publishers.
- Expanded focused message-cycle verification now also passes: `20 passed` for `tests/test_message_cycle_manager.py`, `tests/test_base_agent_runner.py`, `tests/test_token_buffer_memory.py`, `tests/test_agent_chat_local_models.py`, and `tests/test_generation_service.py`.
- Targeted `pyrefly` is clean for the active message-cycle metadata slice.
- Extended `QueueAgentThoughtEvent` to optionally carry serialized agent-thought payload fields from the publisher, and updated the active CoT/function-calling runners to publish those richer events after create/update instead of forcing downstream id-only re-queries.
- Taught the active easy-ui generate task pipeline to build agent-thought stream responses directly from queue-event metadata first, with the existing sync lookup retained only as a fallback for legacy id-only publishers.
- Expanded focused agent-thought verification now also passes: `21 passed` for `tests/test_easy_ui_generate_task_pipeline.py`, `tests/test_message_cycle_manager.py`, `tests/test_base_agent_runner.py`, `tests/test_token_buffer_memory.py`, `tests/test_agent_chat_local_models.py`, and `tests/test_generation_service.py`.
- Targeted `pyrefly` is clean for the active agent-thought metadata slice.
- Removed the active conversation-name worker’s extra sync read path for conversation/app lookup; it now derives `app_mode`, `tenant_id`, and `app_id` directly from the generate entity and issues a direct conversation-name update.
- Expanded focused message-cycle verification now also passes: `22 passed` for `tests/test_message_cycle_manager.py`, `tests/test_easy_ui_generate_task_pipeline.py`, `tests/test_base_agent_runner.py`, `tests/test_token_buffer_memory.py`, `tests/test_agent_chat_local_models.py`, and `tests/test_generation_service.py`.
- Targeted `pyrefly` is clean for the active conversation-name worker slice.
- Extended `QueueMessageFileEvent` with optional `transfer_method` and `upload_file_id` metadata, and updated the active base-app publisher to include them when the inserted row is already available.
- Taught `MessageCycleManager` to cache message-end file info when file events arrive, and updated the active easy-ui pipeline to reuse that cache for `MessageEndStreamResponse.files` before falling back to the old sync query path.
- Expanded focused easy-ui/message-cycle verification now also passes: `24 passed` for `tests/test_message_cycle_manager.py`, `tests/test_easy_ui_generate_task_pipeline.py`, `tests/test_base_agent_runner.py`, `tests/test_token_buffer_memory.py`, `tests/test_agent_chat_local_models.py`, and `tests/test_generation_service.py`.
- Targeted `pyrefly` is clean for the active message-end file cache slice.

Current status
--------------
- The active FastAPI runtime slice is type-clean under targeted `pyrefly` checks.
- The current verified FastAPI route surface is:
  - `/health`
  - `/health/live`
  - `/health/ready`
  - `/console/api/ping`
  - `/console/api/version`
  - `/console/api/system-features`
  - `/api/system-features`
  - `/console/api/init`
  - `/console/api/setup`
  - `/api/webapp/access-mode`
  - `/api/webapp/permission`
  - `/api/site`
  - `/api/parameters`
  - `/api/meta`
  - `/api/passport`
  - `/api/login/status`
  - `/api/logout`
  - `/api/login`
  - `/api/email-code-login`
  - `/api/email-code-login/validity`
  - `/api/forgot-password`
  - `/api/forgot-password/validity`
  - `/api/forgot-password/resets`
  - `/api/remote-files/{url}`
  - `/api/remote-files/upload`
  - `/api/files/upload`
  - `/api/conversations`
  - `/api/conversations/{conversation_id}`
  - `/api/conversations/{conversation_id}/name`
  - `/api/conversations/{conversation_id}/pin`
  - `/api/conversations/{conversation_id}/unpin`
  - `/api/messages`
  - `/api/messages/{message_id}/feedbacks`
  - `/api/saved-messages`
  - `/api/saved-messages/{message_id}`
  - `/api/completion-messages`
  - `/api/chat-messages`
  - `/api/workflows/run`
  - `/api/completion-messages/{task_id}/stop`
  - `/api/chat-messages/{task_id}/stop`
  - `/api/workflows/tasks/{task_id}/stop`
  - `/api/workflow/{task_id}/events`
  - `/api/form/human_input/{form_token}`
  - `/api/audio-to-text`
  - `/api/text-to-audio`
  - `/api/messages/{message_id}/suggested-questions`
  - `/api/messages/{message_id}/more-like-this`
  - `/files/{file_id}/image-preview`
  - `/files/{file_id}/file-preview`
  - `/files/workspaces/{workspace_id}/webapp-logo`
  - `/v1/`
  - `/v1/site`
  - `/v1/parameters`
  - `/v1/meta`
  - `/v1/info`
  - `/v1/files/upload`
  - `/v1/files/{file_id}/preview`
  - `/v1/conversations`
  - `/v1/conversations/{conversation_id}`
  - `/v1/conversations/{conversation_id}/name`
  - `/v1/completion-messages`
  - `/v1/completion-messages/{task_id}/stop`
  - `/v1/chat-messages`
  - `/v1/chat-messages/{task_id}/stop`
  - `/v1/workflows/run`
  - `/v1/workflows/{workflow_id}/run`
  - `/v1/workflows/run/{workflow_run_id}`
  - `/v1/workflows/tasks/{task_id}/stop`
  - `/v1/workflows/logs`
  - `/v1/audio-to-text`
  - `/v1/text-to-audio`
  - `/v1/apps/annotation-reply/{action}`
  - `/v1/apps/annotation-reply/{action}/status/{job_id}`
  - `/v1/apps/annotations`
  - `/v1/apps/annotations/{annotation_id}`
  - `/v1/workspaces/current/models/model-types/{model_type}`
  - `/v1/datasets`
  - `/v1/datasets/{dataset_id}`
  - `/v1/datasets/{dataset_id}/metadata`
  - `/v1/datasets/{dataset_id}/metadata/built-in`
  - `/v1/datasets/{dataset_id}/metadata/{metadata_id}`
  - `/v1/datasets/{dataset_id}/metadata/built-in/{action}`
  - `/v1/datasets/{dataset_id}/documents/metadata`
  - `/v1/datasets/{dataset_id}/documents`
  - `/v1/datasets/{dataset_id}/documents/{batch}/indexing-status`
  - `/v1/datasets/{dataset_id}/documents/{document_id}`
  - `/v1/datasets/{dataset_id}/documents/{document_id}/download`
  - `/v1/datasets/tags`
  - `/v1/datasets/tags/binding`
  - `/v1/datasets/tags/unbinding`
  - `/v1/datasets/{dataset_id}/tags`
  - `/v1/app/feedbacks`
  - `/v1/conversations/{conversation_id}/variables`
  - `/v1/conversations/{conversation_id}/variables/{variable_id}`
  - `/v1/messages`
  - `/v1/messages/{message_id}/feedbacks`
  - `/v1/messages/{message_id}/suggested`
  - `/v1/end-users/{end_user_id}`
- The copied legacy tree is not ported yet; it still contains Flask-specific imports and synchronous `db.session` usage.
- The active `api-new` dependency set no longer includes Flask, `flask-login`, or `flask-restx`.
- The previous import-time blocker from `models/__init__.py` has been removed for the new runtime slice.
- Workflow event replay now stays on local FastAPI models/services; it no longer imports Flask response objects or legacy workflow controllers.
- Public human-input form handling now stays on local FastAPI models/services with async SQLAlchemy and Redis-backed rate limits.
- Public audio handling now stays on FastAPI route/service code and uses the model runtime directly instead of the legacy Flask audio controller path.
- Public suggested-questions handling now stays on FastAPI route/service code with local async conversation/message lookups.
- Public `more-like-this` now routes through the async-native completion generation service.
- Public webapp auth extraction now uses `fastapi.security` helpers for bearer/header/cookie credentials in the active runtime.
- The initial `/v1` service API slice now uses `fastapi.security` bearer auth plus local async app/token/end-user/site lookups under `api_server/`.
- The expanded `/v1` service API slice now also handles app metadata, service-API file upload, and app-scoped file preview through local FastAPI services and ORM models under `api_server/`.
- The expanded `/v1` service API slice now also reuses the native FastAPI conversation/message and suggested-question services via service-API end-user resolution and local runtime-context construction.
- The expanded `/v1` service API slice now also reuses the native FastAPI conversation list/delete/rename services for service-API chat applications instead of the legacy sync conversation controller path.
- The expanded `/v1` service API slice now also reuses the native FastAPI completion/chat generation service and Redis stop-flag task control for service-API callers instead of the legacy Flask `AppGenerateService` and task-stop controllers.
- The expanded `/v1` service API slice now also reuses the native FastAPI workflow generation/task-control path and local async workflow-run lookup service instead of the legacy Flask workflow run/detail/stop controllers.
- The expanded `/v1` service API slice now also reuses the native FastAPI audio transcription/synthesis service paths instead of the legacy Flask audio controllers.
- The expanded `/v1` service API slice now also exposes app-wide feedback listing through a local async query and supports workflow-id-specific runs by threading a workflow override through the native FastAPI workflow runner.
- The expanded `/v1` service API slice now also handles conversation-variable listing and updates through local async SQLAlchemy plus graph variable serialization, instead of the legacy sync conversation-variable service path.
- The expanded `/v1` service API slice now also serves workflow execution logs through local async workflow-log and workflow-run mappings instead of the legacy Flask `WorkflowAppService`.
- The expanded `/v1` service API slice now also handles annotation CRUD plus enable/disable job dispatch and status polling through local async services and owner-account lookup, instead of the legacy Flask annotation service and console permission glue.
- The expanded `/v1` service API slice now also handles the dataset-token workspace model listing route through a native FastAPI dataset-token auth path instead of the legacy Flask dataset-token wrapper.
- The expanded `/v1` service API slice now also serves the read-only dataset metadata endpoints through local dataset-token auth plus minimal async dataset metadata models/services, instead of the legacy Flask metadata controller path.
- The expanded `/v1` service API slice now also handles dataset metadata create/update/delete and built-in-field toggling through local async dataset/document metadata services instead of the legacy Flask metadata mutation controllers.
- The expanded `/v1` service API slice now also handles bulk document metadata updates through the same local async dataset/document metadata service path instead of the legacy Flask metadata bulk-update controller.
- The expanded `/v1` service API slice now also handles dataset tag listing, CRUD, binding, and per-dataset tag status through local async tag/dataset services instead of the legacy Flask tag management controllers.
- The expanded `/v1` service API slice now also serves the basic dataset document list/detail/indexing-status endpoints through local async dataset/document queries instead of the legacy Flask document read controllers.
- The expanded `/v1` service API slice now also serves document download URLs through local async dataset/document/upload-file lookups plus the existing signed-file helper instead of the legacy Flask document download controller.
- The expanded `/v1` service API slice now also serves the basic dataset list/detail endpoints through local async dataset, tag, metadata, and count queries instead of the legacy Flask dataset list/detail controllers.
- Public completion/chat generation routes now validate/authenticate on FastAPI and route through an async-native service layer.
- Public workflow generation no longer hard-fails at the route layer and now runs through the workflow runner without Flask compatibility glue.
- Public advanced-chat generation no longer hard-fails at the route layer and now runs through a direct runner/task-pipeline path without the Flask controller bridge.
- Public agent-chat generation no longer hard-fails at the route layer and now runs through a direct runner/task-pipeline path without the Flask controller bridge.
- The active FastAPI public generation service no longer retains a direct Flask compatibility fallback.
- The execution engine now has an async event-emission surface, but advanced-chat/workflow still need an async-native app service built on top of it.
- The workflow runners now expose async entrypoints, but advanced-chat/workflow still need async-native persistence and response services built on top of them.
- The graph engine now has async-native queue/control-plane primitives, but node execution and workflow persistence still are not fully ported end to end.
- The graph nodes now expose an async execution entrypoint, but most node implementations still execute synchronously behind that interface and workflow persistence is still not async-native end to end.
- The plugin/model client now has async request surfaces for key LLM operations, but the node/model runtime stack still needs to consume those async methods end to end.
- The lower model-runtime/model-instance abstractions now expose async methods, but concrete nodes still need to switch from sync invokes to those async paths end to end.
- The main LLM-backed nodes, `http_request`, and `tool` now have async execution paths, but the broader node/runtime layer still needs the same treatment for any remaining concrete nodes on real workflow paths.
- The copied workflow/advanced-chat/agent-chat runtime still contains sync-only persistence and helper paths, but the active public generation request stage now loads and persists its local FastAPI models on `AsyncSession`; the configured sync companion session factory is now concentrated deeper at the copied runner/repository/pipeline boundary.
- The shared workflow response/pipeline helpers used by the active FastAPI runtime now rely on the configured sync session factory instead of probing the async engine directly.
- The active workflow response/log slice now uses local FastAPI `WorkflowRun` and `WorkflowAppLog` mappings instead of the legacy ORM classes for those entities, reducing active runtime coupling to the copied Flask-era workflow models.
- The active workflow pause-token helper and pause-response form lookups now use local FastAPI human-input models instead of the legacy ORM classes for those entities.
- The active public workflow generation entrypoint now loads local FastAPI `App`, `Workflow`, and `EndUser` models, and the copied workflow runtime/config helpers on that path now accept those local model shapes.
- The active public advanced-chat generation path now uses local FastAPI `App`, `Workflow`, `EndUser`, `Conversation`, `Message`, and `ConversationVariable` models, with local model helpers covering the copied advanced-chat runtime’s required fields and variable conversions.
- The active public agent-chat entry path now loads local FastAPI `App`, `AppModelConfig`, `EndUser`, `Conversation`, and `Message` models, and the local conversation/message helpers now cover the copied token-memory and agent-config behavior that path requires.
- The active inner agent-chat runtime now also uses explicit sync sessions plus local FastAPI `MessageAgentThought`, `MessageFile`, and `UploadFile` mappings on the public path, rather than relying on `db.session` globals for those operations.
- The shared active generation/config layer now accepts local FastAPI `AppMode` and local FastAPI workflow/advanced-chat/agent-chat model shapes on the public path, so the remaining live legacy dependency surface is concentrated deeper in the copied runtime modules rather than at the FastAPI entry/model layer.
- The shared message-cycle helper on the active advanced/agent path now reads local FastAPI `Conversation` and `MessageFile` rows instead of the legacy ORM classes.
- The copied chat/completion config and runner entry signatures now also accept local FastAPI `App`, `AppModelConfig`, `Conversation`, and `Message` shapes, and local `AppModelConfig.to_dict()` now carries the legacy `prompt_type` field they expect.
- The shared chat/completion/agent runtime helpers now type-check and operate against the local FastAPI model shapes on the active path, with the remaining live legacy dependency surface pushed further inward into deeper copied generator/runtime modules.
- The shared message-based generator layer and the copied chat/completion generators now also accept local FastAPI app/config/conversation/message shapes, and the copied chat-path conversation lookup no longer depends on legacy ORM classes on that entry path.
- The copied agent/chat/completion/workflow/advanced-chat/pipeline generator layers now use the configured sync session factory at the database boundary and no longer depend on Flask request-context helpers, though they are still sync-first and not part of the active FastAPI route surface.
- The remaining Flask imports are concentrated in copied legacy generator/controller paths that are no longer on the active public FastAPI route surface.

Immediate blockers
------------------
- Large parts of the copied code still assume:
  - Flask request/session globals
  - Flask-Login
  - sync SQLAlchemy sessions via `db.session`
  - Flask-specific controllers and decorators
- Many services import broad legacy modules rather than small interfaces, so route-by-route porting still requires extracting local async models/services under `api_server/`.
- Model-provider resolution and the broader generation feature set still depend on copied legacy domain code in places, so the async-native generation slice is only partially ported compared with the original Flask feature set.
- The remaining unfinished work is concentrated in moving workflow-backed generation persistence/response flows onto the new async execution surface end to end and shrinking the remaining sync companion-engine seams underneath the copied runtime.

Next steps
----------
1. Continue porting the next public route family.
   - Candidate next slice: move the public workflow-backed generation persistence/response flow from sync companion helpers to async-native execution/persistence services.
   - Prefer local async models/services under `api_server/` rather than extending Flask-era services.

2. Expand the FastAPI-native domain layer.
   - Continue extracting minimal async models/services for features actually exposed by routes.
   - Keep new runtime code under `api_server/` instead of extending Flask-era modules.

3. Port controller groups incrementally.
   - Bootstrap/authentication
   - Console public endpoints
   - Web public endpoints
   - Console authenticated endpoints
   - Service API endpoints

4. Replace sync DB access.
   - Eliminate the remaining sync companion-engine dependencies from active FastAPI code paths after the copied workflow runtime is ported end to end.
   - Move active repositories/services to `AsyncSession`.

5. Replace Flask-specific infrastructure.
   - Auth/session wrappers
   - Error translation
   - request-context helpers
   - extension bootstrapping

6. Widen verification after each slice.
   - `pyrefly` on the active FastAPI package
   - pytest on newly ported routes/services
   - only after a package is active, run broader type coverage on it

Definition of done
------------------
- No active FastAPI code imports Flask or Flask-only extensions.
- Active routes use FastAPI request handling and async SQLAlchemy.
- Targeted `pyrefly` passes for every active package.
- Route/service tests pass for the active surface.
- Legacy copied files are either ported, isolated from runtime, or removed.
